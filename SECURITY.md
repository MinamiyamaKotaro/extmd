# セキュリティポリシー

## サポート対象バージョン

`extmd`はセマンティックバージョニングに従います。脆弱性修正は
[crates.io](https://crates.io/crates/extmd)で公開中の最新バージョンに対してのみ行い、
過去バージョンへのバックポートは行いません。

| バージョン | サポート状況 |
| --- | --- |
| 最新版 | ✅ |
| それ以前 | ❌ |

## 脆弱性の報告方法

脆弱性を発見した場合は、公開のIssueやPull Requestではなく、GitHubの
[Private vulnerability reporting](https://github.com/MinamiyamaKotaro/extmd/security/advisories/new)
から報告してください。第三者に影響が及ぶ前に対応するため、非公開 of 報告経路を使用してください。

報告には可能な範囲で以下の情報を含めてください。

- 影響を受けるバージョン
- 再現手順（可能であればサンプルの`.xlsx`ファイルや入力）
- 想定される影響範囲

## 依存関係の脆弱性チェック

依存クレートの既知脆弱性は[RustSec Advisory Database](https://rustsec.org/)を用いた
`cargo audit`により、`Cargo.toml`/`Cargo.lock`を変更するPRで自動チェックしています
（[`.github/workflows/security-audit.yml`](.github/workflows/security-audit.yml)）。

---

## Web API / サーバー環境における統合ガイドライン

`extmd` は単発で実行される CLI ツールまたはスタンドアロンのライブラリとして設計されています。
本ツールを Web API サーバーやバックグラウンドワーカーなどのサーバーサイド環境に統合して、アップロードされた `.xlsx` ファイルをオンデマンドで変換する場合、同時並行実行に伴うシステムリソースの枯渇（DoS 脆弱性）を防ぐため、以下の設計ガイドラインおよびリソース制御を講じることを強く推奨します。

### 1. リソース消費の測定データ（ベンチマーク）

機械的に生成した一般的な表形式のデータ構造を持つスプレッドシート（奇数列: テキスト、偶数列: フォーマットされた数値）に対して `extmd` を実行し、実行時間および最大物理メモリ使用量（Peak RSS）を測定した結果は以下の通りです。

| セル数（行 × 列） | ファイル名 | 実行時間 (Real Time) | 最大物理メモリ使用量 (Peak RSS) |
| :--- | :--- | :--- | :--- |
| **10,000** (100 × 100) | `bench_10k.xlsx` | 0.15s | 16.51 MB |
| **50,000** (500 × 100) | `bench_50k.xlsx` | 0.15s | 44.46 MB |
| **100,000** (500 × 200) | `bench_100k.xlsx` | 0.34s | 76.90 MB |
| **500,000** (1,000 × 500) | `bench_500k.xlsx` | 1.74s | 331.15 MB |
| **1,000,000** (1,000 × 1,000) | `bench_1m.xlsx` | 3.69s | 642.62 MB |

* 測定環境: Apple M1 Max / 32GB RAM / macOS 15

#### 特性の分析
* **メモリ使用量 (Peak RSS)**: セル数に対して**ほぼ線形にスケーリング**します（目安として **1,000 セルあたり約 0.65 MB**）。
* **実行時間**: パースおよびレンダリング時間もセル数に対して**ほぼ線形にスケーリング**します（目安として **1,000 セルあたり約 3.7 ms**）。
* CLI デフォルト値である `--max-cells 1000000`（100万セル）のファイルをパースする場合、単一リクエストで約 640 MB のメモリを占有し、数秒間 1スレッド of CPU を 100% 占有します。同時多重リクエストが発生するサーバー環境では、メモリ枯渇を招くリスクがあります。

---

### 2. 推奨されるセキュリティ・リソース制御設計

#### ① サンドボックス / サブプロセスとしての分離実行
* `extmd` の変換ロジックを Web サーバープロセス自体に直接ライブラリリンクして実行するのではなく、**外部サブプロセス（例: `tokio::process::Command` など）として CLI を実行する**アプローチを推奨します。
* **理由**: 
  * メモリフラグメンテーションの蓄積を完全に OS レベルでクリアできる。
  * 万が一パニックや想定外の Out-Of-Memory (OOM) が発生した場合でも、親である Web サーバープロセスのクラッシュ（巻き込み）を防止できる。

#### ② `--max-cells` 上限の引き下げ
* サーバーサイドでは、CLI 起動時のオプションに `--max-cells` を明示指定し、デフォルトの 1,000,000 から大幅に引き下げることを推奨します。
* **設定例**:
  ```sh
  # 最大セル数を 100,000 に制限して実行（Peak RAM を約 80MB 以下、処理時間を約 0.3秒前後に抑制）
  extmd input.xlsx --max-cells 100000
  ```
* これにより、悪意ある巨大なファイルのアップロードによる OOM や CPU 独占攻撃を早期にエラー終了させることができます。

#### ③ 実行タイムアウトの設定
* サブプロセス実行時に、利用側のコードで必ず厳格なタイムアウト（例: 3〜5秒）を設けてください。
* 指定時間内に終了しなかった場合は、プロセスに対して `SIGKILL` を送信して強制終了させてください。

#### ④ 同時実行数の制限（ワーカープールの制御）
* API ゲートウェイやタスクキューのレイヤーにおいて、`extmd` を同時に並行実行する数をシステム（CPU コア数など）に合わせて制限（セマフォ制御など）してください。
* 無制限に並行実行を許容すると、複数のリクエストで一斉にメモリが消費され、ホスト OS の OOM Killer が発動する原因となります。

#### ⑤ エラーメッセージのサニタイズ
* `extmd` が出力する標準エラー出力やパースエラーのメッセージには、入力ファイルのパスや内部パーサの構造など、システム内部の情報が断片的に含まれる場合があります。
* これらのエラーログはサーバー側の監視用ログ（プライベート）にのみ記録し、Web API のクライアントへ返却するレスポンスメッセージには「ファイルのパースに失敗しました」等の定型メッセージのみを返すようにサニタイズしてください。
